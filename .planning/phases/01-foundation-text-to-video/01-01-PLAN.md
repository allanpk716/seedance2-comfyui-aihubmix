---
phase: 01-foundation-text-to-video
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - __init__.py
  - requirements.txt
  - tests/conftest.py
  - tests/test_registration.py
autonomous: true
requirements:
  - PFND-01
  - PFND-02
  - PFND-03

must_haves:
  truths:
    - "ComfyUI discovers the plugin via NODE_CLASS_MAPPINGS in __init__.py"
    - "All node classes have CATEGORY set to 'Seedance 2.0'"
    - "requirements.txt lists all runtime dependencies with correct minimum versions"
  artifacts:
    - path: "__init__.py"
      provides: "Plugin registration with ComfyUI"
      contains: "NODE_CLASS_MAPPINGS"
    - path: "requirements.txt"
      provides: "Dependency declaration for pip install"
      contains: "requests"
    - path: "tests/test_registration.py"
      provides: "Automated verification of registration and dependencies"
      contains: "test_node_class_mappings"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
      contains: "import pytest"
  key_links:
    - from: "__init__.py"
      to: "nodes.py"
      via: "from .nodes import"
      pattern: "from \\.nodes import"
    - from: "requirements.txt"
      to: "pip"
      via: "ComfyUI Manager runs pip install -r requirements.txt"
      pattern: "requests>="
---

<objective>
Create the plugin scaffold with ComfyUI registration and dependency declaration.

Purpose: Establish the project structure and ComfyUI node registration pattern so subsequent plans can add API client logic and node implementations against a working skeleton.

Output: `__init__.py` with placeholder node imports, `requirements.txt` with all dependencies, test infrastructure (`tests/conftest.py`, `tests/test_registration.py`) with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-text-to-video/01-CONTEXT.md
@.planning/phases/01-foundation-text-to-video/01-RESEARCH.md
@.planning/phases/01-foundation-text-to-video/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test infrastructure and plugin registration tests (RED)</name>
  <files>tests/conftest.py, tests/test_registration.py, tests/__init__.py</files>
  <read_first>
    - seedance_video.py (reference for project patterns)
    - .planning/phases/01-foundation-text-to-video/01-RESEARCH.md (ComfyUI registration pattern)
    - .planning/phases/01-foundation-text-to-video/01-VALIDATION.md (test IDs and commands)
  </read_first>
  <behavior>
    - test_node_class_mappings: Importing __init__ module exposes NODE_CLASS_MAPPINGS dict containing keys "SeedanceApiKey" and "SeedanceTextToVideo"
    - test_node_display_name_mappings: NODE_DISPLAY_NAME_MAPPINGS dict exists with display names for both nodes
    - test_node_categories: Both node classes (imported via NODE_CLASS_MAPPINGS) have CATEGORY == "Seedance 2.0"
    - test_requirements: requirements.txt file exists and contains lines matching "requests>=", "Pillow>=", "numpy>=", "opencv-python>="
  </behavior>
  <action>
    Create the test infrastructure for Phase 1:

    1. Create `tests/__init__.py` as an empty file (makes tests/ a Python package).

    2. Create `tests/conftest.py` with shared pytest fixtures:
       - `mock_api_key`: Returns a test API key string "test-api-key-12345"
       - `mock_video_response`: Returns a dict mimicking a completed AIHubMix API response with fields: id="vid_test123", status="completed", progress=100, video_url="https://example.com/video.mp4"
       - `mock_pending_response`: Returns a dict with status="in_progress", progress=50
       - `mock_failed_response`: Returns a dict with status="failed", error={"message": "Test error"}

    3. Create `tests/test_registration.py` with these test functions:
       - `test_node_class_mappings`: Import the __init__ module (using `importlib.import_module` since the project root is the package), verify NODE_CLASS_MAPPINGS is a dict containing "SeedanceApiKey" and "SeedanceTextToVideo" as keys, and the values are classes.
       - `test_node_display_name_mappings`: Verify NODE_DISPLAY_NAME_MAPPINGS is a dict with "SeedanceApiKey" and "SeedanceTextToVideo" keys, and values are non-empty strings.
       - `test_node_categories`: For each class in NODE_CLASS_MAPPINGS.values(), assert `cls.CATEGORY == "Seedance 2.0"`.
       - `test_requirements`: Read requirements.txt, assert it contains lines matching "requests>=", "Pillow>=", "numpy>=", "opencv-python>=". Do NOT assert torch (ComfyUI manages its own PyTorch).

    Since nodes.py does not exist yet, create a minimal stub `nodes.py` with placeholder classes so the tests can import:

    ```python
    class SeedanceApiKey:
        CATEGORY = "Seedance 2.0"

    class SeedanceTextToVideo:
        CATEGORY = "Seedance 2.0"
    ```

    And a minimal `__init__.py`:
    ```python
    from .nodes import SeedanceApiKey, SeedanceTextToVideo

    NODE_CLASS_MAPPINGS = {
        "SeedanceApiKey": SeedanceApiKey,
        "SeedanceTextToVideo": SeedanceTextToVideo,
    }

    NODE_DISPLAY_NAME_MAPPINGS = {
        "SeedanceApiKey": "Seedance 2.0 API Key",
        "SeedanceTextToVideo": "Seedance 2.0 Text to Video",
    }

    __all__ = ["NODE_CLASS_MAPPINGS", "NODE_DISPLAY_NAME_MAPPINGS"]
    ```

    And a `requirements.txt`:
    ```
    requests>=2.28.0
    Pillow>=9.0.0
    numpy>=1.23.0
    opencv-python>=4.7.0
    ```

    Run tests with `pytest tests/test_registration.py -x`. They MUST pass (this is the GREEN step since we need the stubs for downstream plans to import).
  </action>
  <acceptance_criteria>
    - File `tests/conftest.py` exists and contains `import pytest`
    - File `tests/test_registration.py` exists and contains `def test_node_class_mappings` and `def test_node_requirements`
    - File `__init__.py` exists and contains `NODE_CLASS_MAPPINGS` and `NODE_DISPLAY_NAME_MAPPINGS`
    - File `nodes.py` exists and contains `class SeedanceApiKey` and `class SeedanceTextToVideo`
    - File `requirements.txt` exists and contains `requests>=2.28.0` and `Pillow>=9.0.0` and `opencv-python>=4.7.0`
    - Command `pytest tests/test_registration.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd C:/WorkSpace/seedance_comfyui && python -m pytest tests/test_registration.py -x -q</automated>
  </verify>
  <done>All 4 registration tests pass. Plugin skeleton files exist with correct structure. Test infrastructure fixtures are reusable by Plans 02 and 03.</done>
</task>

</tasks>

<verification>
Run the full test suite for this plan:
```bash
cd C:/WorkSpace/seedance_comfyui && python -m pytest tests/test_registration.py -v
```
All 4 tests must pass:
- test_node_class_mappings
- test_node_display_name_mappings
- test_node_categories
- test_requirements
</verification>

<success_criteria>
- `__init__.py` exports NODE_CLASS_MAPPINGS with "SeedanceApiKey" and "SeedanceTextToVideo"
- `__init__.py` exports NODE_DISPLAY_NAME_MAPPINGS with display names
- `nodes.py` has placeholder classes for SeedanceApiKey and SeedanceTextToVideo with CATEGORY = "Seedance 2.0"
- `requirements.txt` lists requests, Pillow, numpy, opencv-python with minimum versions
- All 4 registration tests pass via `pytest tests/test_registration.py -v`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-text-to-video/01-01-SUMMARY.md`
</output>
