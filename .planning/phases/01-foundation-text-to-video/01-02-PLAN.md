---
phase: 01-foundation-text-to-video
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - api_client.py
  - tests/test_api_client.py
autonomous: true
requirements:
  - APIC-01
  - APIC-02
  - APIC-03
  - APIC-04
  - APIC-05

must_haves:
  truths:
    - "API client creates video tasks via POST /v1/videos with correct model ID and auth header"
    - "API client polls GET /v1/videos/{id} every 5 seconds and logs progress to console"
    - "HTTP 401/402/429 responses raise RuntimeError with actionable error messages"
    - "Network failures during polling are caught and logged without crashing"
    - "The model identifier doubao-seedance-2-0-260128 is used for all requests"
  artifacts:
    - path: "api_client.py"
      provides: "All AIHubMix HTTP communication"
      exports: ["create_video", "wait_for_video", "create_and_wait", "SIZE_MAP"]
    - path: "tests/test_api_client.py"
      provides: "Unit tests for API client with mocked HTTP"
      contains: "test_create_video"
  key_links:
    - from: "api_client.py"
      to: "https://aihubmix.com/v1"
      via: "requests.post/get"
      pattern: "requests\\.(post|get).*aihubmix\\.com"
    - from: "api_client.py"
      to: "nodes.py"
      via: "imported and called by T2V node"
      pattern: "from \\.api_client import"
---

<objective>
Build the AIHubMix API client module with create, poll, and error handling.

Purpose: Provide a clean, testable API interaction layer that all generation nodes will call. This module encapsulates all HTTP communication with AIHubMix so nodes never call requests directly.

Output: `api_client.py` with functions `create_video`, `wait_for_video`, `create_and_wait`, plus SIZE_MAP and error handling. All functions tested with mocked HTTP responses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-text-to-video/01-CONTEXT.md
@.planning/phases/01-foundation-text-to-video/01-RESEARCH.md
@.planning/phases/01-foundation-text-to-video/01-VALIDATION.md
@seedance_video.py
</context>

<interfaces>
<!-- Key constants and patterns the executor must use. Extracted from test script and CONTEXT.md decisions. -->

Module-level constants (per RESEARCH.md Pattern 3):
```python
BASE_URL = "https://aihubmix.com/v1"
MODEL = "doubao-seedance-2-0-260128"
```

SIZE_MAP (copy exactly from seedance_video.py lines 44-57):
```python
SIZE_MAP = {
    ("480p", "16:9"): "832x480",
    ("480p", "9:16"): "480x832",
    ("480p", "1:1"):  "624x624",
    ("480p", "4:3"):  "640x480",
    ("480p", "3:4"):  "480x640",
    ("480p", "21:9"): "1120x480",
    ("720p", "16:9"): "1280x720",
    ("720p", "9:16"): "720x1280",
    ("720p", "1:1"):  "960x960",
    ("720p", "4:3"):  "960x720",
    ("720p", "3:4"):  "720x960",
    ("720p", "21:9"): "1680x720",
}
```

Error messages (per D-08 Chinese messages):
- 401: "认证失败 (401)：请检查你的 API 密钥是否正确，可在 aihubmix.com 查看"
- 402: "余额不足 (402)：请到 aihubmix.com 充值"
- 429: "请求过于频繁 (429)：请稍后重试"
- network: "网络连接失败：请检查网络设置"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write API client tests then implement api_client.py</name>
  <files>tests/test_api_client.py, api_client.py</files>
  <read_first>
    - seedance_video.py (the proven API interaction pattern to adapt)
    - .planning/phases/01-foundation-text-to-video/01-CONTEXT.md (locked decisions D-01 through D-09)
    - .planning/phases/01-foundation-text-to-video/01-RESEARCH.md (Pattern 3: API Client Module)
    - tests/conftest.py (fixtures from Plan 01, if available; otherwise create inline)
  </read_first>
  <behavior>
    - test_auth_header: create_video() sends Authorization header "Bearer {api_key}" via mocked requests.post
    - test_model_id: create_video() sends payload with model="doubao-seedance-2-0-260128" via mocked requests.post
    - test_create_video_success: create_video() with valid response returns the video ID string
    - test_create_video_no_id: create_video() raises RuntimeError when response has no id or video_id field
    - test_create_video_size_1080p: create_video() with resolution="1080p" does NOT include "size" in payload
    - test_create_video_size_720p: create_video() with resolution="720p" ratio="16:9" includes size="1280x720" in payload
    - test_polling_completed: wait_for_video() with mocked sequence (queued -> in_progress -> completed) returns result dict
    - test_polling_failed: wait_for_video() raises RuntimeError when status becomes "failed"
    - test_polling_timeout: wait_for_video() raises RuntimeError when max_wait is exceeded
    - test_polling_network_error: wait_for_video() catches RequestException, logs, and retries
    - test_error_401: _check_response with 401 status raises RuntimeError containing "认证失败"
    - test_error_402: _check_response with 402 status raises RuntimeError containing "余额不足"
    - test_error_429: _check_response with 429 status raises RuntimeError containing "请求过于频繁"
    - test_create_and_wait: create_and_wait() orchestrates create_video + wait_for_video and returns dict with video_url and video_id
  </behavior>
  <action>
    **Step 1: RED - Create tests first.**

    Create `tests/test_api_client.py` using `unittest.mock.patch` to mock `requests.post` and `requests.get`.

    Test structure:
    - Use `@patch("api_client.requests.post")` and `@patch("api_client.requests.get")` for all HTTP mocking.
    - Use `conftest.py` fixtures if available; otherwise define inline mock data.
    - Import from `api_client`: `create_video`, `wait_for_video`, `create_and_wait`, `_check_response`, `SIZE_MAP`, `MODEL`.

    Key test details:
    - `test_auth_header`: Mock post returning 200 with {"id": "test123"}. Inspect mock_post.call_args[1]["headers"]["Authorization"] equals "Bearer test-key".
    - `test_model_id`: Same mock. Inspect mock_post.call_args[1]["json"]["model"] equals "doubao-seedance-2-0-260128".
    - `test_create_video_size_1080p`: Call create_video with resolution="1080p". Assert "size" not in mock_post.call_args[1]["json"].
    - `test_create_video_size_720p`: Call with resolution="720p", ratio="16:9". Assert mock_post.call_args[1]["json"]["size"] == "1280x720".
    - `test_polling_completed`: Mock get returning sequence: {"status": "queued"}, {"status": "in_progress", "progress": 50}, {"status": "completed", "progress": 100, "video_url": "https://example.com/v.mp4"}. Assert returns dict with video_url.
    - `test_polling_failed`: Mock get returning {"status": "failed", "error": {"message": "Content violation"}}. Assert raises RuntimeError containing "Content violation".
    - `test_polling_timeout`: Use max_wait=1 and poll_interval=2 to trigger timeout. Assert raises RuntimeError containing "timed out".
    - `test_polling_network_error`: Mock get raising requests.RequestException("Connection error"). With max_wait > poll_interval so it retries at least once before hitting another condition. The test should verify the function does NOT crash on a single network error (it logs and retries).
    - `test_error_401`: Create a mock response with status_code=401, text="Unauthorized". Pass to _check_response. Assert raises RuntimeError containing "认证失败".
    - `test_error_402`: Same with 402, assert "余额不足".
    - `test_error_429`: Same with 429, assert "请求过于频繁".
    - `test_create_and_wait`: Mock both create_video and wait_for_video (patch at module level). Verify returns {"video_url": "...", "video_id": "..."}.

    Run tests: `pytest tests/test_api_client.py -x` -- they MUST fail (api_client.py does not exist yet).

    **Step 2: GREEN - Implement api_client.py.**

    Create `api_client.py` adapting patterns from `seedance_video.py` (the test script). Key changes from the test script:
    - Remove `sys.exit()` calls -- use `raise RuntimeError()` instead (per D-09)
    - Add Chinese error messages per D-08
    - Remove CLI-specific code (argparse, main function)
    - Remove download_video function (Phase 2 scope)
    - Remove image encoding functions (Phase 2 scope)
    - Replace sys.stdout.write progress bar with plain print() per D-03/D-04
    - Add max_wait parameter with 600s default
    - Make api_key a function parameter (not global constant)

    Module structure:
    ```python
    """AIHubMix API client for Seedance 2.0 video generation."""
    import time
    import requests

    BASE_URL = "https://aihubmix.com/v1"
    MODEL = "doubao-seedance-2-0-260128"

    SIZE_MAP = { ... }  # Copy exactly from seedance_video.py

    def _get_headers(api_key): ...
    def _get_size(resolution, ratio): ...
    def _check_response(resp): ...  # Chinese error messages per D-08
    def create_video(api_key, prompt, duration=5, resolution="1080p", ratio="16:9"): ...
    def wait_for_video(api_key, video_id, poll_interval=5, max_wait=600): ...
    def create_and_wait(api_key, prompt, duration=5, resolution="1080p", ratio="16:9"): ...
    ```

    Specific implementation details:
    - `_check_response(resp)`: Check status_code 401/402/429 first with Chinese messages. For other non-200 codes, call `resp.raise_for_status()`.
    - `create_video`: POST to `{BASE_URL}/videos`. Extract video_id from `data.get("id") or data.get("video_id")`. Raise RuntimeError if missing.
    - `wait_for_video`: Poll GET `{BASE_URL}/videos/{video_id}` every poll_interval seconds. Use plain `print(f"[Seedance] {status} - {progress}%")` for progress. Catch `requests.RequestException` and log + continue. Raise RuntimeError on "failed" status or timeout.
    - `create_and_wait`: Call create_video then wait_for_video. Return dict with "video_url" (from `result.get("video_url") or result.get("url", "")`) and "video_id".

    Run tests: `pytest tests/test_api_client.py -x` -- all tests MUST pass.

    **Step 3: REFACTOR if needed.**

    Clean up any duplication. Run `pytest tests/test_api_client.py -v` to confirm all pass.
  </action>
  <acceptance_criteria>
    - File `api_client.py` exists at project root
    - `api_client.py` contains `BASE_URL = "https://aihubmix.com/v1"`
    - `api_client.py` contains `MODEL = "doubao-seedance-2-0-260128"`
    - `api_client.py` contains `SIZE_MAP` dict with at least 12 entries (6 for 480p, 6 for 720p)
    - `api_client.py` contains `def create_video(api_key`
    - `api_client.py` contains `def wait_for_video(api_key`
    - `api_client.py` contains `def create_and_wait(api_key`
    - `api_client.py` contains "认证失败" in the 401 error message
    - `api_client.py` contains "余额不足" in the 402 error message
    - `api_client.py` contains "请求过于频繁" in the 429 error message
    - `api_client.py` does NOT contain `sys.exit`
    - File `tests/test_api_client.py` exists and contains at least 14 test functions
    - Command `pytest tests/test_api_client.py -v` exits 0 with all tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd C:/WorkSpace/seedance_comfyui && python -m pytest tests/test_api_client.py -v</automated>
  </verify>
  <done>All 14 API client tests pass. api_client.py provides create_video, wait_for_video, create_and_wait with SIZE_MAP. Error messages follow D-08 Chinese format. No sys.exit calls.</done>
</task>

</tasks>

<verification>
Run the full API client test suite:
```bash
cd C:/WorkSpace/seedance_comfyui && python -m pytest tests/test_api_client.py -v
```
All tests must pass:
- test_auth_header
- test_model_id
- test_create_video_success
- test_create_video_no_id
- test_create_video_size_1080p
- test_create_video_size_720p
- test_polling_completed
- test_polling_failed
- test_polling_timeout
- test_polling_network_error
- test_error_401
- test_error_402
- test_error_429
- test_create_and_wait
</verification>

<success_criteria>
- `api_client.py` implements all HTTP communication with AIHubMix
- Auth header is `Authorization: Bearer {api_key}` (APIC-02)
- Model ID is `doubao-seedance-2-0-260128` in all requests (APIC-05)
- Polling loops every 5 seconds with print-based progress (APIC-03, D-03/D-04)
- Error messages for 401/402/429/network include Chinese actionable text (APIC-04, D-08)
- No sys.exit() calls -- all errors via raise RuntimeError/ValueError (D-09)
- SIZE_MAP covers all 12 valid 480p/720p combinations
- 1080p omits size field (API default)
- All 14 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-text-to-video/01-02-SUMMARY.md`
</output>
